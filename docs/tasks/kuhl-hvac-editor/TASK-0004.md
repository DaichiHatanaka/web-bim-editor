# TASK-0004: 空間ノードスキーマ（Plant, Building, Level）

**タスクID**: TASK-0004
**タスクタイプ**: TDD
**推定工数**: 8時間
**フェーズ**: Phase 1 - 基盤構築
**信頼性レベル**: 🔵 *設計文書 §2.1 + interfaces.ts*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/kuhl-hvac-editor/requirements.md) - REQ-004
- **設計文書**: [📐 architecture.md](../../design/kuhl-hvac-editor/architecture.md)

## タスク概要

PlantNode(`plant_xxx`)、BuildingNode(`building_xxx`)、LevelNode(`level_xxx`)の Zod スキーマを定義。空間階層（Plant → Building → Level）の parentId 関係を表現。

## 依存タスク

- **前提タスク**: [TASK-0003](TASK-0003.md)
- **後続タスク**: [TASK-0008](TASK-0008.md)

## 完了条件

- [ ] PlantNode, BuildingNode, LevelNode スキーマが定義されている
- [ ] `.parse()` で正しいID形式のノードが生成される
- [ ] テスト60%以上カバレッジ

---

## 実装詳細

### 1. PlantNode スキーマ 🔵

**信頼性**: 🔵 *設計文書 §2.1 + interfaces.ts PlantNode*

id: `plant_${string}`, type: 'plant', plantName, address?, children (Building IDs)。

**実装ファイル**: `packages/kuhl-core/src/schema/nodes/plant.ts`

### 2. BuildingNode スキーマ 🔵

**信頼性**: 🔵 *既存実装 + 設計文書 §2.1*

id: `building_${string}`, type: 'building', buildingName, children (Level IDs)。

**実装ファイル**: `packages/kuhl-core/src/schema/nodes/building.ts`

### 3. LevelNode スキーマ 🔵

**信頼性**: 🔵 *既存実装 + 設計文書 §2.1*

id: `level_${string}`, type: 'level', floorHeight (m), ceilingHeight (m), elevation (m GL基準), children (HvacZone IDs)。

**実装ファイル**: `packages/kuhl-core/src/schema/nodes/level.ts`

---

## 単体テスト要件

### テストケース1: PlantNode.parse() 成功・ID形式検証 🔵

**Given**: 有効な PlantNode データ
**When**: PlantNode.parse() 実行
**Then**: `plant_` で始まるIDが生成される

### テストケース2: BuildingNode.parse() 成功・parentId 検証 🔵

**Given**: 有効な BuildingNode データ (parentId = plant_xxx)
**When**: BuildingNode.parse() 実行
**Then**: 正常にパース

### テストケース3: LevelNode.parse() 成功・数値フィールド検証 🔵

**Given**: elevation=3.5, floorHeight=3.2, ceilingHeight=2.7
**When**: LevelNode.parse() 実行
**Then**: 正常にパース、数値が保持される

### テストケース4: 不正入力バリデーションエラー 🔵

**Given**: type が不正な値
**When**: parse() 実行
**Then**: ZodError 発生

**テストファイル**: `packages/kuhl-core/src/__tests__/schema/nodes/spatial.test.ts`

---

## 実装手順

### TDDタスク

1. `/tsumiki:tdd-requirements TASK-0004` - 詳細要件定義
2. `/tsumiki:tdd-testcases` - テストケース作成
3. `/tsumiki:tdd-red` - テスト実装（失敗）
4. `/tsumiki:tdd-green` - 最小実装
5. `/tsumiki:tdd-refactor` - リファクタリング
6. `/tsumiki:tdd-verify-complete` - 品質確認

---

## 注意事項

- 既存の Building, Level との名前衝突に注意（@kuhl/core 内で独立）
- children は ID 配列（フラット辞書のため実体は持たない）

---

## 信頼性レベルサマリー

| カテゴリ | 🔵 青 | 🟡 黄 | 🔴 赤 | 合計 |
|---------|-------|-------|-------|------|
| 実装詳細 | 3 | 0 | 0 | 3 |
| 単体テスト | 4 | 0 | 0 | 4 |

### 全体評価

- **総項目数**: 7項目
- 🔵 **青信号**: 7項目 (100%)
- 🟡 **黄信号**: 0項目 (0%)
- 🔴 **赤信号**: 0項目 (0%)

**品質評価**: 高品質
