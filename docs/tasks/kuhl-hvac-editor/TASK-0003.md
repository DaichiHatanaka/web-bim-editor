# TASK-0003: BaseNode・共通スキーマフォーク

**タスクID**: TASK-0003
**タスクタイプ**: TDD
**推定工数**: 8時間
**フェーズ**: Phase 1 - 基盤構築
**信頼性レベル**: 🔵 *既存packages/core/src/schema/base.ts + interfaces.ts*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/kuhl-hvac-editor/requirements.md) - REQ-007, REQ-010
- **設計文書**: [📐 architecture.md](../../design/kuhl-hvac-editor/architecture.md)

## タスク概要

@pascal-app/core の BaseNode スキーマを @kuhl/core にフォーク。Zod 4 で BaseNode, CameraSchema を定義。objectId()（nanoidプレフィックス付きID生成）、nodeType()（リテラル型ヘルパー）を実装。

## 依存タスク

- **前提タスク**: [TASK-0001](TASK-0001.md)
- **後続タスク**: [TASK-0004](TASK-0004.md), [TASK-0005](TASK-0005.md), [TASK-0006](TASK-0006.md)

## 完了条件

- [ ] BaseNode スキーマが Zod 4 で定義されている
- [ ] `.parse()` でノード生成できる
- [ ] objectId() がプレフィックス付きIDを生成する
- [ ] テスト60%以上カバレッジ

---

## 実装詳細

### 1. BaseNode Zodスキーマ 🔵

**信頼性**: 🔵 *既存 packages/core/src/schema/base.ts より*

object, id, type, name, parentId, visible, camera, metadata フィールドを Zod 4 で定義。

**実装ファイル**: `packages/kuhl-core/src/schema/base.ts`

### 2. CameraSchema 🔵

**信頼性**: 🔵 *既存実装より*

position, target ([number,number,number] タプル), zoom を定義。

**実装ファイル**: `packages/kuhl-core/src/schema/camera.ts`

### 3. objectId() / nodeType() ヘルパー 🔵

**信頼性**: 🔵 *既存実装パターンより*

objectId(prefix): `{prefix}_{nanoid(10)}` 形式のID生成。nodeType(literal): Zodリテラル型ヘルパー。

**実装ファイル**: `packages/kuhl-core/src/schema/base.ts`

---

## 単体テスト要件

### テストケース1: BaseNode.parse() 成功・失敗 🔵

**信頼性**: 🔵 *既存テストパターンより*

**Given**: 有効/無効なBaseNodeデータ
**When**: BaseNode.parse() 実行
**Then**: 有効→正常にパース、無効→ZodError

### テストケース2: objectId() プレフィックス付きID生成 🔵

**信頼性**: 🔵 *既存実装より*

**Given**: prefix = "ahu"
**When**: objectId("ahu") 実行
**Then**: `ahu_` で始まるID文字列を返す

### テストケース3: nodeType() リテラル型 🔵

**信頼性**: 🔵 *既存実装より*

**Given**: literal = "ahu"
**When**: nodeType("ahu") 実行
**Then**: z.literal("ahu") と同等のZodスキーマを返す

### テストケース4: CameraSchema 検証 🔵

**信頼性**: 🔵 *既存実装より*

**Given**: position/target の3要素配列
**When**: CameraSchema.parse() 実行
**Then**: 正常にパース

**テストファイル**: `packages/kuhl-core/src/__tests__/schema/base.test.ts`

---

## 実装手順

### TDDタスク

1. `/tsumiki:tdd-requirements TASK-0003` - 詳細要件定義
2. `/tsumiki:tdd-testcases` - テストケース作成
3. `/tsumiki:tdd-red` - テスト実装（失敗）
4. `/tsumiki:tdd-green` - 最小実装
5. `/tsumiki:tdd-refactor` - リファクタリング
6. `/tsumiki:tdd-verify-complete` - 品質確認

---

## 注意事項

- 既存 @pascal-app/core のコードをそのままコピーせず、必要な部分のみフォーク
- Zod 4 の API で実装（Zod 3 との差異に注意）
- `@kuhl/core` は React/Three.js 非依存を維持

---

## 信頼性レベルサマリー

| カテゴリ | 🔵 青 | 🟡 黄 | 🔴 赤 | 合計 |
|---------|-------|-------|-------|------|
| 実装詳細 | 3 | 0 | 0 | 3 |
| 単体テスト | 4 | 0 | 0 | 4 |

### 全体評価

- **総項目数**: 7項目
- 🔵 **青信号**: 7項目 (100%)
- 🟡 **黄信号**: 0項目 (0%)
- 🔴 **赤信号**: 0項目 (0%)

**品質評価**: 高品質
